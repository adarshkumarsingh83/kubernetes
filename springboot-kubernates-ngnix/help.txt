# To build the docker image and tag
$ docker build -f Dockerfile -t adarshkumarsingh83/springboot-web-rest .

# to push the docker image to the docker hub 
$ docker push adarshkumarsingh83/springboot-web-rest

#to create nginx image and push
$ docker build -f  Dockerfile -t adarshkumarsingh83/nginx .

$ docker push adarshkumarsingh83/nginx

$  kubectl apply -f /Users/akumar38/GITHUB/kubernetes/springboot-kubernates-ngnix/springboot.yaml

$ kubectl apply -f /Users/akumar38/GITHUB/kubernetes/springboot-kubernates-ngnix/ngnix.yaml

$ kubectl get service frontend --watch


$ curl http://${EXTERNAL_IP}/

# for api using terminal
$ curl localhost:8080/api/message

# for Ui in browser
http://localhost:8080/


$ kubectl delete services frontend springboot-web-rest


$ kubectl delete deployment frontend springboot-web-rest


# TO DELETE ALL THE CONTAINERS WITH VOLUMES
$ docker rm -vf $(docker ps -a -q)

# TO DELETE ALL THE IMAGES
$ docker rmi -f $(docker images -a -q)